<!DOCTYPE html>
<html>
  <head>
    <title>LevelUp</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= stylesheet_link_tag 'topBar', media: 'all', 'data-turbo-track': 'reload' %>
    <%= stylesheet_link_tag 'tasks', media: 'all', 'data-turbo-track': 'reload' if current_page?(root_path) %>
    


    <%= javascript_include_tag "application", type: "module", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

<div class="top-bar">
  <!-- EXP Bar -->
  <div class="top-bar-item exp-bar-container">
    <div class="exp-bar">
      <div class="exp-bar-progress" style="width: <%= current_user.exp_percentage %>%;">
        <span class="exp-bar-text">Level <%= current_user.level %> | <%= current_user.exp %>/<%= current_user.exp_required %></span>
      </div>
    </div>
  </div>

  <!-- Coins -->
  <div class="top-bar-item currency-container">
    💰 <span><%= current_user.coins %></span>
  </div>

  <!-- Diamonds -->
  <div class="top-bar-item currency-container">
    💎 <span><%= current_user.diamonds %></span>
  </div>
</div>


    
    <%= yield %>
    
  </body>
</html>



