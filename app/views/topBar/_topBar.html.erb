<div class="top-bar">
    <!-- EXP Bar -->
    <div class="exp-bar-container">
        <div class="exp-bar-progress" style="width: <%= current_user.exp_percentage %>%;">
        </div>
        <span class="exp-bar-text">
          lv.  <%= current_user.level %> <%= current_user.exp %>/<%= current_user.exp_required %> exp
        </span>
    </div>


  <!-- Coins -->
  <div class="top-bar-item currency-bar">
    <div class="currency-bar-text">
      💰 <span><%= current_user.coins %></span>
    </div>
  </div>

  <!-- Diamonds -->
  <div class="top-bar-item currency-bar diamond-bar">
    <div class="currency-bar-text">
      💎 <span><%= current_user.diamonds %></span>
    </div>
  </div>
</div>
