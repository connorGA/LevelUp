{
  "dependencies": {
    "@hotwired/turbo-rails": "^8.0.12",
    "@rails/ujs": "^7.1.3-4",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --format=esm --sourcemap --outdir=app/assets/builds",
    "build:watch": "esbuild app/javascript/application.js --bundle --format=esm --sourcemap --outdir=app/assets/builds --watch"
  }
}
